<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.dubs.whatscooking.whatscooking">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".WelcomeActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".LandingActivity"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".RestaurantMenuActivity"
            android:label="Menu" />
        <activity android:name=".MainActivity"
            android:label="Restaurant Selector"/>
        <activity android:name=".RecommendationsActivity"
            android:label="Your Recommendations"/>
        <activity android:name=".SuccessConfirmRating" />
        <activity
            android:name=".WebViewActivity"
            android:label="Recipe"
            android:theme="@style/AppTheme.NoActionBar"/>
    </application>

</manifest>